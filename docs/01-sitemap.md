# A. サイトマップ・ページ構成

## A-1. 全体ナビゲーション

新サイト（WordPress + SWELL）のグローバルナビゲーション構成：

```
┌─ Gスタ撮影会 ロゴ
│
├─ TOP                      /
├─ イベント                 /event/
│   ├─ セッション           /event/category/session/
│   ├─ SP撮影会             /event/category/sp/
│   ├─ 個撮                 /event/category/one/
│   ├─ キカク               /event/category/kikaku/
│   └─ スペシャル           /event/category/special/
│
├─ モデル一覧               /model/
│   └─ モデル詳細           /model/{slug}/
│
├─ 初めての方へ             /beginners/
├─ アクセス                 /access/
├─ お申し込み               /apply/
├─ お問い合わせ             /contact/
└─ モデル募集               /audition/
```

### URL設計方針

- イベントは WP カスタム投稿タイプ `event` のスラッグを使用
- カテゴリは WP カスタムタクソノミー `event_category`
- モデルは WP カスタム投稿タイプ `model`
- 旧URL `/event/27397/` は **そのまま継承可能**（数値スラッグで保持）
  → 旧サイトからのSEO・SNSシェアの資産を温存

## A-2. 現行サイトとの対応関係

現行サイト（613ページ）の構造を新サイトに集約：

| 現行 | 件数 | 新サイトでの扱い |
|---|---|---|
| `/event/{ID}/` | 約150件 | カスタム投稿タイプ `event` として移行（同URL継承） |
| `/one/{ID}/` | 約120件 | `event` に統合（カテゴリ=個撮）+ `/one/{ID}/` 301リダイレクト |
| 固定ページ19件 | 19 | 一部廃止、現役のものだけ移植 |
| `/page/2~6/` | 5 | WP標準のページネーション |
| `/feed/` | — | WP標準RSSで代替 |

### ✅ 新サイトに移植する固定ページ

- 初めての方へ（`/beginners/`）
- アクセス（`/access/`）
- モデル募集（`/audition/`）
- お申し込み（`/apply/`）
- お問い合わせ（`/contact/`）

### ❓ Q03: 廃止候補ページ（要確認）

- WJガールズオーディション系（第2回〜第6回）
- 近代麻雀水着祭
- checklist（用途不明）

## A-3. TOPページ構成

上から順に：

```
1. ヒーロー（SWELLのメインビジュアル機能 + カスタマイズ）
   - メインビジュアル（スライダー or 静止画）
   - キャッチコピー
   - メインCTAボタン × 2

2. NEWS / お知らせ                       ❓ Q14 要確認
   - 最新3件くらい

3. カテゴリナビ（5種）
   - SWELLのブロック「カラム」+ 背景画像で実装

4. ピックアップイベント
   - SWELLの「投稿リスト」ブロック or 自作テンプレート
   - カテゴリフィルタ・直近順表示

5. 開催スケジュール（カレンダー）        🚧 Q15 実装方法決定要
   - 候補1: ACF日付フィールド + 自作カレンダーUI
   - 候補2: プラグイン「The Events Calendar」
   - 候補3: シンプルなリスト表示で代替

6. 初めての方へ
   - SWELLのバナー or 3カードブロック

7. モデルピックアップ
   - 丸サムネ（CSS調整）+ 投稿リストブロック
   - カスタム投稿タイプ `model` から取得

8. アクセス
   - SWELL Google Map ブロック

9. フッター
   - SWELLウィジェット + サイトマップ
```

## A-4. ページごとの主要要素

### TOPページ
詳細は A-3 参照。

### イベントカテゴリページ（`/event/category/{slug}/`）
- WP カテゴリアーカイブテンプレート（`taxonomy-event_category.php`）
- カテゴリ説明文（タクソノミー説明）
- 並び順: ACF日付フィールド近い順

### イベント詳細ページ（`/event/{slug}/`）
- カスタム投稿タイプ `event` シングルテンプレート（`single-event.php`）
- ACFフィールド: メインバナー、日付、時刻、会場、料金、出演モデル（リレーション）
- 申し込みボタン

### モデル一覧（`/model/`）
- カスタム投稿タイプ `model` のアーカイブ（`archive-model.php`）
- 50音順 or 事務所別フィルタ
- 丸サムネイルグリッド

### モデル詳細（`/model/{slug}/`）
- `single-model.php`
- ACFフィールド: プロフィール写真、所属事務所、SNS、Bio
- 過去出演イベント一覧（リレーションの逆引き）

### 初めての方（`/beginners/`）
- 固定ページ + SWELLブロックエディタで構築
- ステップ別ガイド・FAQ

### アクセス（`/access/`）
- 固定ページ
- Google Map 埋め込み・住所・営業時間

## A-5. URL設計方針

### ✅ 旧URL継承

WordPressへの移行なら、旧URL `/event/27397/` を**そのまま使えます**。
カスタム投稿タイプのスラッグを数値ID名で登録すれば良いだけ。

→ 301リダイレクトの大量設定が不要、SEO・SNSシェア資産を完全保全。

`/one/{ID}/` のみ、新カテゴリ「個撮」配下の `/event/{ID}/` に統一するため、
301リダイレクトを設定（`.htaccess` で正規表現1行）。

## A-6. レスポンシブ対応方針

- モバイルファースト（SWELL標準）
- ブレイクポイント: SWELLデフォルト（599px / 959px）
- ターゲットユーザー（中年男性）配慮:
  - 本文最小16px（SWELLカスタマイザーで設定）
  - ボタン高さ最小44px
  - 行間1.7以上

---

**未解決項目**: `00-questions.md` の Q03, Q13, Q14, Q15, Q17 を参照
